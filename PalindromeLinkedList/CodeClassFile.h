#pragma once

#include "../std_lib_facilities.h"
#include <stack>

struct ListNode {
    int val;
    ListNode *next;
    ListNode(int x) : val(x), next(NULL) {}
};

class Solution {
public:
	bool isPalindrome(ListNode* head) 
	{
		stack<int> myStack;
		while (head != NULL)
		{
			
		}
	}
};